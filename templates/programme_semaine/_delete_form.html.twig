<form method="post" action="{{ path('app_programme_semaine_delete', {'id': programme_semaine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programme_semaine.id) }}">
    <button class="btn">Delete</button>
</form>
