<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Commande #{{ commande.id }}</title>
    <style>
        /* Ajoutez ici les styles CSS pour votre PDF */
    </style>
</head>
<body>
<h1>Commande #{{ commande.id }}</h1>

<table>
    <thead>
    <tr>
        <th>Id</th>
        <th>Date_commande</th>
        <th>Nom_commande</th>
        <th>actions</th>
    </tr>
    </thead>
    <tbody>
    <tr>
        <td>{{ commande.id }}</td>
        <td>{{ commande.dateCommande|date('d/m/Y') }}</td>
        <td>{{ commande.nomCommande }}</td>
        <td><!-- Ajoutez ici les actions pour cette commande --></td>
    </tr>
    </tbody>
    <tfoot>
    <tr>
        <th colspan="3">Total</th>
        <td><!-- Ajoutez ici le total pour cette commande --></td>
    </tr>
    </tfoot>
</table>
</body>
</html>