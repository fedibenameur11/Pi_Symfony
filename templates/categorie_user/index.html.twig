{% extends 'base.html.twig' %}

{% block title %}CategorieUser index{% endblock %}

{% block body %}
    <h1>CategorieUser index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_user in categorie_users %}
            <tr>
                <td>{{ categorie_user.id }}</td>
                <td>{{ categorie_user.nom }}</td>
                <td>
                    <a href="{{ path('app_categorie_user_show', {'id': categorie_user.id}) }}">show</a>
                    <a href="{{ path('app_categorie_user_edit', {'id': categorie_user.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_user_new') }}">Create new</a>
{% endblock %}
