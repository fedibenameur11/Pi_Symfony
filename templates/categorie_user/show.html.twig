{% extends 'base2.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_user.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ categorie_user.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_user_index') }}">List</a>

    <a href="{{ path('app_categorie_user_edit', {'id': categorie_user.id}) }}">Modifier</a>

    {{ include('categorie_user/_delete_form.html.twig') }}
{% endblock %}
