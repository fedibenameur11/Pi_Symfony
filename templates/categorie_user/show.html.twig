{% extends 'base.html.twig' %}

{% block title %}CategorieUser{% endblock %}

{% block body %}
    <h1>CategorieUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_user.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie_user.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_user_index') }}">back to list</a>

    <a href="{{ path('app_categorie_user_edit', {'id': categorie_user.id}) }}">edit</a>

    {{ include('categorie_user/_delete_form.html.twig') }}
{% endblock %}
