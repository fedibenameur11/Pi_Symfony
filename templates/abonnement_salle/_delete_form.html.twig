<form method="post" action="{{ path('app_abonnement_salle_delete', {'id': abonnement_salle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ abonnement_salle.id) }}">
    <button class="btn">Supprimer</button>
</form>
